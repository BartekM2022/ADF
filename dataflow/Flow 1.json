{
	"name": "Flow 1",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"name": "SourceAzureBlob1"
				}
			],
			"sinks": [],
			"transformations": [
				{
					"name": "unionBlobSQL"
				},
				{
					"name": "lookup1"
				},
				{
					"name": "join12"
				},
				{
					"name": "select1"
				},
				{
					"name": "rank1"
				}
			],
			"scriptLines": [
				"source(allowSchemaDrift: true,",
				"     validateSchema: false) ~> AzureBlob1",
				"AzureBlob1 union(byName: true)~> unionBlobSQL",
				"unionBlobSQL lookup(ERROR_FUNCTION('') == ERROR_FUNCTION(''),",
				"     multiple: false,",
				"     pickup: 'any',",
				"     broadcast: 'auto')~> lookup1",
				"lookup1 join(ERROR_FUNCTION('') == ERROR_FUNCTION(''),",
				"     joinType:'inner',",
				"     matchType:'exact',",
				"     ignoreSpaces: false,",
				"     broadcast: 'auto')~> join1",
				"join1 select(skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> select1",
				"select1 rank(asc(ERROR_FUNCTION(''), true),",
				"     output(undefined as long)) ~> rank1"
			]
		}
	}
}